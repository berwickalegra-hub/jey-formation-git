---
phase: 00-foundation
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - frontend/package.json
  - pnpm-lock.yaml
  - frontend/vitest.config.ts
  - frontend/src/lib/server/observability/.gitkeep
autonomous: true
requirements:
  - OPS-02
tags:
  - foundation
  - tooling
  - vitest

must_haves:
  truths:
    - "pnpm test runs Vitest 2.1.8 with @/* alias resolution working"
    - "fast-glob 3.3.3 is available for the runtime-enforcement guard test"
    - "@vercel/otel 2.1.2 is installed without unmet peer-dependency blockers"
    - "frontend/src/lib/server/observability/ directory exists in the repo"
  artifacts:
    - path: "frontend/vitest.config.ts"
      provides: "Vitest config with @/* alias and node environment"
      contains: "defineConfig"
    - path: "frontend/package.json"
      provides: "Updated dependencies (@vercel/otel, fast-glob)"
      contains: "@vercel/otel"
    - path: "frontend/src/lib/server/observability/.gitkeep"
      provides: "Placeholder so the directory exists for parallel Wave-1 plans to write into"
      min_lines: 0
  key_links:
    - from: "frontend/vitest.config.ts"
      to: "frontend/src/**/*.test.ts"
      via: "include glob"
      pattern: "src/\\*\\*/\\*\\.test\\.ts"
    - from: "frontend/vitest.config.ts"
      to: "frontend/src/*"
      via: "@/* alias"
      pattern: "'@'"
---

<objective>
Wave 0 foundation: install the two new dependencies (`@vercel/otel`, `fast-glob`) and create the Vitest configuration + the `lib/server/observability/` directory that every other Phase 0 plan writes into. This plan unblocks all of Wave 1 (Plans 02–05 are file-disjoint and can run in parallel only after this one lands).

Purpose: Without `vitest.config.ts` the `@/*` alias does not resolve in tests; without `fast-glob` the runtime guard test cannot walk the filesystem; without the `observability/` directory parallel writes from later plans collide on the missing parent. Without `@vercel/otel` Plan 03 cannot import `registerOTel`.

Output: New deps in `frontend/package.json` + lockfile, a minimal `frontend/vitest.config.ts`, and the `frontend/src/lib/server/observability/` directory tracked via `.gitkeep`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/00-foundation/00-CONTEXT.md
@.planning/phases/00-foundation/00-RESEARCH.md
@.planning/phases/00-foundation/00-VALIDATION.md
@frontend/package.json

<interfaces>
<!-- Wave-1 plans depend on this exact shape. Do not deviate. -->

vitest.config.ts MUST resolve the `@/*` alias to `./src` so test files can `import { … } from '@/lib/server/...'`.

Example shape (from RESEARCH.md Example 4, lines 595–611):
```typescript
import { defineConfig } from 'vitest/config';
import path from 'node:path';

export default defineConfig({
  test: {
    include: ['src/**/*.test.ts'],
    environment: 'node',
  },
  resolve: {
    alias: {
      '@': path.resolve(__dirname, './src'),
    },
  },
});
```

Dependency-add command (from RESEARCH.md "Installation" section, lines 154–164):
```bash
pnpm --filter frontend add @vercel/otel
pnpm --filter frontend add -D fast-glob
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add @vercel/otel and fast-glob dependencies to frontend</name>
  <read_first>
    - frontend/package.json (current dependency list — verify @vercel/otel and fast-glob are NOT already present)
    - .planning/phases/00-foundation/00-RESEARCH.md (lines 118–164: dep table + install commands; lines 504–507: peer-dep pitfall #5)
    - .planning/phases/00-foundation/00-CONTEXT.md (D-16: registerOTel from @vercel/otel; D-05: fast-glob for guard test)
  </read_first>
  <files>frontend/package.json, pnpm-lock.yaml</files>
  <action>
    Run from repo root:
    1. `pnpm --filter frontend add @vercel/otel@^2.1.2`
    2. `pnpm --filter frontend add -D fast-glob@^3.3.3`

    Then run `pnpm install` from the repo root and inspect stdout for the line `WARN  Issues with peer dependencies found`. If pnpm flags any of the OpenTelemetry SDK peers (`@opentelemetry/api`, `@opentelemetry/sdk-trace-base`, `@opentelemetry/resources`) as missing AND `pnpm dev` fails on import (run `pnpm dev` for ~5 seconds and Ctrl-C), install them explicitly with: `pnpm --filter frontend add @opentelemetry/api @opentelemetry/sdk-trace-base @opentelemetry/resources`. Pitfall 5 of RESEARCH.md (line 504) documents the symptom (`Cannot find module '@opentelemetry/...'` at startup).

    Do NOT modify any other file. The `postinstall` hook (`prisma generate`) will run automatically — that is fine and expected.
  </action>
  <verify>
    <automated>node -e "const p = require('./frontend/package.json'); if (!p.dependencies['@vercel/otel']) { console.error('missing @vercel/otel'); process.exit(1); } if (!p.devDependencies['fast-glob']) { console.error('missing fast-glob'); process.exit(1); } console.log('ok'); "</automated>
  </verify>
  <acceptance_criteria>
    - frontend/package.json `dependencies` contains a key `@vercel/otel` whose value starts with `^2.1` (e.g. `^2.1.2`)
    - frontend/package.json `devDependencies` contains a key `fast-glob` whose value starts with `^3.3` (e.g. `^3.3.3`)
    - pnpm-lock.yaml is updated and committed (git diff shows lockfile additions)
    - `pnpm install` exits 0 (lockfile in sync)
    - No edits to any other file in the repo
  </acceptance_criteria>
  <done>Both deps appear under their correct sections in `frontend/package.json`; lockfile is regenerated; `pnpm install` exits 0 with no peer-dep blockers (warnings acceptable so long as dev/test boot cleanly).</done>
</task>

<task type="auto">
  <name>Task 2: Create frontend/vitest.config.ts (minimal config with @/* alias)</name>
  <read_first>
    - frontend/tsconfig.json (verify `@/*` alias mapping in compilerOptions.paths so the Vitest alias matches)
    - .planning/phases/00-foundation/00-RESEARCH.md (lines 593–613: Example 4 — exact config; assumption A4 line 727)
    - frontend/package.json (confirm vitest 2.1.8 in devDependencies — already present)
  </read_first>
  <files>frontend/vitest.config.ts</files>
  <action>
    Create `frontend/vitest.config.ts` with the EXACT content from RESEARCH.md Example 4 (lines 595–611):

    ```typescript
    // frontend/vitest.config.ts — minimal config; default test discovery.
    import { defineConfig } from 'vitest/config';
    import path from 'node:path';

    export default defineConfig({
      test: {
        include: ['src/**/*.test.ts'],
        environment: 'node',
      },
      resolve: {
        alias: {
          '@': path.resolve(__dirname, './src'),
        },
      },
    });
    ```

    Do not add coverage, setupFiles, globals, or any other option. Phase 6 TEST-01 will extend this with `setupFiles` for JWT_SECRET / ENCRYPTION_KEY fixtures — out of scope here.
  </action>
  <verify>
    <automated>node -e "const fs = require('fs'); const s = fs.readFileSync('frontend/vitest.config.ts', 'utf8'); if (!/from 'vitest\/config'/.test(s)) { console.error('missing vitest/config import'); process.exit(1); } if (!/'@': path\.resolve\(__dirname, '\.\/src'\)/.test(s)) { console.error('missing @ alias'); process.exit(1); } if (!/include: \['src\/\*\*\/\*\.test\.ts'\]/.test(s)) { console.error('missing include glob'); process.exit(1); } if (!/environment: 'node'/.test(s)) { console.error('missing node environment'); process.exit(1); } console.log('ok'); "</automated>
  </verify>
  <acceptance_criteria>
    - File `frontend/vitest.config.ts` exists
    - File imports `defineConfig` from `'vitest/config'`
    - File contains the literal substring `'@': path.resolve(__dirname, './src')`
    - File contains the literal substring `include: ['src/**/*.test.ts']`
    - File contains the literal substring `environment: 'node'`
    - `pnpm --filter frontend test` exits 0 (zero tests is OK at this point — exit 0 confirms config is parseable)
  </acceptance_criteria>
  <done>vitest.config.ts created with the exact shape; `pnpm --filter frontend test` exits 0 (zero tests at this point is fine).</done>
</task>

<task type="auto">
  <name>Task 3: Create frontend/src/lib/server/observability/ directory with .gitkeep</name>
  <read_first>
    - .planning/phases/00-foundation/00-RESEARCH.md (lines 196–205: file-additions tree showing observability/ as new subdirectory; D-14 in CONTEXT.md line 60)
    - frontend/src/lib/server/ (note existing subdirs: admin/, middleware/, notifications/, oauth/, outbox/, payments/, queues/, upload/, webhook/, withdrawals/ — observability/ is new)
  </read_first>
  <files>frontend/src/lib/server/observability/.gitkeep</files>
  <action>
    1. Create the directory `frontend/src/lib/server/observability/` if it does not yet exist (it should not — verify via `ls frontend/src/lib/server/`).
    2. Create an empty `.gitkeep` file inside it: `frontend/src/lib/server/observability/.gitkeep` (zero-byte file is fine; some teams put a single newline — either is acceptable).

    Why a `.gitkeep`: Wave 1 plans (02, 03, 04, 05) write files into this directory in parallel. Git won't track an empty directory; the placeholder ensures the directory exists in the working tree before the parallel plans start writing. Plan 05 will overwrite this with real source files (or the file remains alongside them — harmless either way).
  </action>
  <verify>
    <automated>test -d frontend/src/lib/server/observability && test -f frontend/src/lib/server/observability/.gitkeep && echo ok</automated>
  </verify>
  <acceptance_criteria>
    - Directory `frontend/src/lib/server/observability/` exists
    - File `frontend/src/lib/server/observability/.gitkeep` exists (size 0 or 1 byte)
    - `git status` shows the new file as tracked / staged
  </acceptance_criteria>
  <done>Directory + `.gitkeep` placeholder exist; `git ls-files` includes the path after staging.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pnpm install ↔ npm registry | Untrusted package contents enter the developer machine and CI. Mitigated by lockfile pinning. |
| Vitest test runner ↔ filesystem | Vitest reads source files; no untrusted input crosses into the runtime. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-0-04 | Denial of Service | Postgres pool exhaustion under Vercel concurrency | mitigate (Plan 02 scope; this plan is the prerequisite that lets Plan 02's env-shape test run at all) | Plan 02 enforces `connection_limit=1` in `.env.example` `DATABASE_URL`; this plan only sets up the test framework. |
| T-0-05 | Misconfiguration | Deprecated `experimental.instrumentationHook` config drift | mitigate (Plan 03 scope; this plan provides the Vitest config that Plan 03's `next-config-clean.test.ts` runs under) | Plan 03 writes the assertion test; this plan ensures Vitest can find and run it. |
| T-deps-01 | Tampering (supply chain) | `@vercel/otel`, `fast-glob` packages from npm | accept | Both packages are widely used (`fast-glob` is transitively in Vitest already; `@vercel/otel` is a Vercel-published package). Lockfile pinning via `pnpm-lock.yaml` mitigates registry mutation. Severity: low. |
</threat_model>

<verification>
After all three tasks complete:

1. `pnpm install` from repo root exits 0
2. `pnpm --filter frontend test` exits 0 (no tests yet, but config is parseable)
3. `git status` shows: `frontend/package.json`, `pnpm-lock.yaml`, `frontend/vitest.config.ts`, `frontend/src/lib/server/observability/.gitkeep` as the only modifications
4. `node -e "require('@vercel/otel')"` from `frontend/` directory does NOT throw a `Cannot find module` error (smoke check that the dep resolves)
</verification>

<success_criteria>
- `frontend/package.json` declares `@vercel/otel` ^2.1.x in `dependencies` AND `fast-glob` ^3.3.x in `devDependencies`
- `frontend/vitest.config.ts` exists with the exact alias / include / environment shape from RESEARCH.md Example 4
- `frontend/src/lib/server/observability/` directory exists in the working tree (and is tracked via `.gitkeep`)
- `pnpm install` reports no peer-dep blockers (warnings allowed; failures forbidden)
- All Wave 1 plans (02, 03, 04, 05) can now write to `frontend/src/lib/server/observability/*.test.ts` and have their tests discovered + executed by Vitest
</success_criteria>

<output>
After completion, create `.planning/phases/00-foundation/00-01-SUMMARY.md`
</output>
