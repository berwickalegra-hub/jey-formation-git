---
phase: 01-auth-routes
plan: 05
type: execute
wave: 3
depends_on: ['01-02', '01-03', '01-04']
files_modified:
  - frontend/.env.example
autonomous: true
requirements: [AUTH-10]
must_haves:
  truths:
    - ".env.example documents all 11 new AUTH_* env vars + PASSWORD_HIBP_CHECK with sensible defaults + comments"
    - "Each env var includes the D-XX or AUTH-XX rationale so a fork operator knows WHY"
    - "Phase 1 final gate passes: pnpm format && pnpm lint && pnpm typecheck && pnpm test all exit 0"
    - "Phase 0 runtime-enforcement test sees all 9 new route files have runtime='nodejs' (still green)"
    - "All 10 phase REQ-IDs (AUTH-01 through AUTH-10) covered across plans 01-04"
  artifacts:
    - path: "frontend/.env.example"
      provides: "Documented env reference for all Phase 1 tunables"
      contains: "AUTH_LOGIN_RATE_LIMIT_MAX"
  key_links:
    - from: "frontend/.env.example"
      to: "Phase 1 routes"
      via: "Each env var maps to a process.env.* read in a route or lib helper"
      pattern: "AUTH_"
---

<objective>
Phase 1 closeout — document all 11 new env vars in `.env.example` and run the final phase gate (format, lint, typecheck, full test suite). This is a single-task plan because it's pure documentation + a verification gate; runs in Wave 3 after all 9 routes + Wave 0 lib helpers are landed.

Purpose: A fork operator copying `.env.example` to `.env` knows EXACTLY what each `AUTH_*` value tunes, what its default is, and which decision (D-XX) or requirement (AUTH-XX) ties to it. No guessing, no hunting through commit history.

Output: `.env.example` extended with 11 documented entries; full Phase 1 verification gate green.

Decision coverage: D-08 (rate limit values), D-13 (PASSWORD_HIBP_CHECK off by default), AUTH-10 (per-email rate limits + lockout); also covers the env documentation half of D-06, D-07, D-10.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-auth-routes/01-CONTEXT.md
@.planning/phases/01-auth-routes/01-RESEARCH.md (Runtime State Inventory section — list of 11 env vars)
@.planning/phases/00-foundation/00-02-SUMMARY.md (Phase 0 .env.example shape — preserve existing entries)

<env_vars_to_document>
| Var | Default | D-XX / REQ | Purpose |
|-----|---------|------------|---------|
| AUTH_PASSWORD_MIN_LENGTH | 10 | D-10 | Minimum password length on signup / reset / change |
| AUTH_VERIFICATION_TTL_MIN | 15 | D-06 | Verification code TTL (minutes) for email-verify + password-reset codes |
| AUTH_LOCKOUT_THRESHOLD | 5 | D-07 / AUTH-10 | Failed-login attempts before lockout |
| AUTH_LOCKOUT_DURATION_MIN | 15 | D-07 / AUTH-10 | Lockout duration (minutes) after threshold reached |
| AUTH_LOGIN_RATE_LIMIT_MAX | 10 | D-08 / AUTH-10 | Login rate limit per email per 15-minute window |
| AUTH_SIGNUP_RATE_LIMIT_MAX | 5 | D-08 / AUTH-01 / AUTH-10 | Signup rate limit per email per 1-hour window |
| AUTH_FORGOT_RATE_LIMIT_MAX | 3 | D-08 / AUTH-07 / AUTH-10 | Forgot-password rate limit per email per 1-hour window |
| AUTH_RESET_RATE_LIMIT_MAX | 5 | D-08 / AUTH-08 / AUTH-10 | Reset-password rate limit per email per 15-minute window |
| AUTH_RESEND_VERIFY_RATE_LIMIT_MAX | 3 | D-08 / AUTH-10 | Resend-verification rate limit (reserved for future endpoint; documented now to keep .env.example complete) |
| AUTH_VERIFY_RATE_LIMIT_MAX | 5 | D-08 / AUTH-03 / AUTH-10 | Verify-email rate limit per email per 15-minute window |
| PASSWORD_HIBP_CHECK | (unset = off) | D-13 / AUTH-09 | Set to "1" to enable opt-in HIBP haveibeenpwned password check on signup / reset / change-password (k-anonymity, fail-open on network error) |
</env_vars_to_document>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document 11 env vars in .env.example + final phase gate</name>
  <files>
    frontend/.env.example
  </files>
  <read_first>
    - frontend/.env.example (Phase 0 — preserve DATABASE_URL, DIRECT_URL, CRON_SECRET, JWT_SECRET, ENCRYPTION_KEY, COOKIE_PREFIX, etc.)
    - .planning/phases/01-auth-routes/01-RESEARCH.md (Runtime State Inventory — confirms which 11 vars to add)
    - .planning/phases/01-auth-routes/01-CONTEXT.md (D-08 with the value table)
  </read_first>
  <action>
    1. **Read current `frontend/.env.example`** — confirm existing variables (DATABASE_URL pooler, DIRECT_URL, CRON_SECRET, JWT_SECRET, ENCRYPTION_KEY, etc. from Phase 0). DO NOT remove or rewrite any existing entry.
    2. **Append a new section** to `frontend/.env.example` (after existing entries; group comment for clarity):
       ```
       # ─────────────────────────────────────────────────────────────────
       # Phase 1 — Auth tunables
       # All values are per-fork overridable. Defaults (shown) are
       # production-safe per CONTEXT.md decision IDs (D-06, D-07, D-08,
       # D-10, D-13).
       # ─────────────────────────────────────────────────────────────────

       # Minimum password length (D-10, NIST 800-63B aligned).
       AUTH_PASSWORD_MIN_LENGTH=10

       # Verification code TTL in minutes (D-06). Codes are 8-char Crockford
       # alphanumeric, single-use (consumed via VerificationCode.usedAt).
       AUTH_VERIFICATION_TTL_MIN=15

       # Failed-login lockout (D-07, AUTH-10).
       # After AUTH_LOCKOUT_THRESHOLD failures within AUTH_LOCKOUT_DURATION_MIN
       # minutes, the account is locked out for AUTH_LOCKOUT_DURATION_MIN minutes.
       # Backed by Redis sliding-window counter (memory fallback in dev).
       AUTH_LOCKOUT_THRESHOLD=5
       AUTH_LOCKOUT_DURATION_MIN=15

       # Per-email rate limits (D-08, AUTH-10). Keyed on lower-cased email.
       # Falls back to per-IP when email is missing (parse failure / malformed body).
       AUTH_LOGIN_RATE_LIMIT_MAX=10              # 10 / 15 min per email
       AUTH_SIGNUP_RATE_LIMIT_MAX=5              # 5 / 1 hour per email
       AUTH_FORGOT_RATE_LIMIT_MAX=3              # 3 / 1 hour per email
       AUTH_RESET_RATE_LIMIT_MAX=5               # 5 / 15 min per email
       AUTH_RESEND_VERIFY_RATE_LIMIT_MAX=3       # 3 / 1 hour per email (reserved for future resend endpoint)
       AUTH_VERIFY_RATE_LIMIT_MAX=5              # 5 / 15 min per email

       # HIBP haveibeenpwned password check (D-13). Opt-in.
       # Set to "1" to enable. Default OFF.
       # When enabled, signup / reset-password / change-password reject
       # passwords found in known breaches via k-anonymity (first 5 chars of
       # SHA-1 sent to api.pwnedpasswords.com — full password and full hash
       # never leave the server). Fail-open on network error (logs warn).
       PASSWORD_HIBP_CHECK=
       ```
       Use exact comment text — fork operators read this verbatim.
    3. **Run final Phase 1 gate (commands sequenced; each must exit 0):**
       ```
       pnpm --filter frontend format
       pnpm --filter frontend lint
       pnpm --filter frontend typecheck
       pnpm --filter frontend test
       ```
       If any fails, stop and report — do NOT mask errors. The expected outcome on a healthy Phase 1 build:
       - format: writes any pending Prettier reformatting (idempotent on subsequent runs)
       - lint: ESLint exits 0 with no errors / no `any` cast warnings
       - typecheck: tsc exits 0
       - test: vitest reports all tests passing across Phase 0 + Phase 1 (Wave 0 lib + 9 route handlers + their co-located tests)
    4. **Verify all 10 REQ-IDs are covered** — grep each plan's frontmatter `requirements:` field; confirm AUTH-01 through AUTH-10 each appear in at least one plan:
       - Plan 01: AUTH-10
       - Plan 02: AUTH-01, AUTH-03, AUTH-07, AUTH-08
       - Plan 03: AUTH-02, AUTH-04, AUTH-05, AUTH-06, AUTH-10
       - Plan 04: AUTH-09
       - Plan 05: AUTH-10 (env-doc layer)
    5. **Phase 0 guard cross-check:** `pnpm --filter frontend exec vitest run -t "runtime"` — runtime-enforcement test still green; sees all 9 new route files exporting `runtime = 'nodejs'`.
  </action>
  <verify>
    <automated>pnpm --filter frontend format && pnpm --filter frontend lint && pnpm --filter frontend typecheck && pnpm --filter frontend test 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - frontend/.env.example contains 'AUTH_PASSWORD_MIN_LENGTH=10'
    - frontend/.env.example contains 'AUTH_LOCKOUT_THRESHOLD=5' AND 'AUTH_LOCKOUT_DURATION_MIN=15'
    - frontend/.env.example contains 'AUTH_LOGIN_RATE_LIMIT_MAX=10'
    - frontend/.env.example contains 'AUTH_SIGNUP_RATE_LIMIT_MAX=5'
    - frontend/.env.example contains 'AUTH_FORGOT_RATE_LIMIT_MAX=3'
    - frontend/.env.example contains 'AUTH_RESET_RATE_LIMIT_MAX=5'
    - frontend/.env.example contains 'AUTH_VERIFY_RATE_LIMIT_MAX=5'
    - frontend/.env.example contains 'AUTH_VERIFICATION_TTL_MIN=15'
    - frontend/.env.example contains 'PASSWORD_HIBP_CHECK='
    - frontend/.env.example STILL contains 'DATABASE_URL' AND 'DIRECT_URL' AND 'JWT_SECRET' (Phase 0 entries preserved)
    - `pnpm --filter frontend lint` exits 0
    - `pnpm --filter frontend typecheck` exits 0
    - `pnpm --filter frontend test` exits 0
    - `pnpm --filter frontend exec vitest run -t "runtime"` exits 0 (Phase 0 guard sees 9 new route files)
  </acceptance_criteria>
  <done>Phase 1 closed: env vars documented; full gate green; Phase 0 guard still green; all 10 REQ-IDs covered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer → fork operator | Documentation accuracy: incorrect defaults or missing comments lead to misconfigured prod deployments |

## STRIDE Threat Register

| Threat ID | Severity | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|----------|-----------|-------------|-----------------|
| T-1-08 | LOW | Misconfiguration | .env.example | mitigate | Each var has a clear comment + decision-ID reference; safe defaults baked in; PASSWORD_HIBP_CHECK explicitly defaults OFF (D-13) so a fork operator MUST opt-in (avoids surprise external API calls) |
| T-1-04 | LOW | Information Disclosure | .env.example committed to repo | accept | .env.example contains no secrets — only documented var NAMES and DEFAULT values. Real secrets go in .env (gitignored). Phase 0 already enforces this pattern. |
</threat_model>

<verification>
- `frontend/.env.example` extended with all 11 new entries; Phase 0 entries preserved
- Each new entry has a comment referencing D-XX / AUTH-XX
- Final phase gate (format + lint + typecheck + test) all exit 0
- Runtime-enforcement test from Phase 0 sees the 9 new route files and stays green
- All 10 phase REQ-IDs (AUTH-01 through AUTH-10) appear in at least one plan's `requirements:` frontmatter
- No do-not-modify file (auth.ts, crypto.ts, middleware/**) was touched in any Phase 1 plan (verified via git diff against pre-Phase-1 SHA)
</verification>

<success_criteria>
1. Fork operator copying `.env.example` to `.env` understands every AUTH_* var without external research
2. Defaults match D-08 / D-07 / D-10 / D-13 specifications exactly
3. `pnpm --filter frontend test` reports all Phase 0 + Phase 1 tests passing
4. Phase 1 ROADMAP success criteria 1-5 all observable via the test suite + `.env.example` contents
</success_criteria>

<output>
After completion, create `.planning/phases/01-auth-routes/01-05-SUMMARY.md` per template — record:
- 11 env vars documented; defaults table
- Final phase gate output (test count, lint clean, typecheck clean)
- Phase 1 closing notes:
  - 9 routes total: signup, verify-email, login, refresh, logout, me, forgot-password, reset-password, change-password
  - 6 lib helpers in lib/server/auth/ (banned-passwords, hibp, lockout, refresh-lock, dummy-bcrypt, email-templates)
  - OutboxEvent union extended (email.verification_code + email.password_reset)
  - Vitest infra in place (setup file + test-utils mocks) — Phase 6 TEST-01 narrowed to E2E smoke
  - vitest-mock-extended@^4.0.0 added as devDep
- Open items for downstream phases:
  - Phase 5 outbox dispatcher must handle the 2 new email.* event variants (calls verificationEmail + resetPasswordEmail factories)
  - Phase 6 DOC-01 should update CLAUDE.md to reference lib/server/auth/ subdirectory pattern (sibling to do-not-modify auth.ts)
- Patterns established for downstream:
  - "lib/server/<feature>/<helper>.ts" sibling-dir pattern when namesake `.ts` is do-not-modify
  - Pre-session route CSRF carve-out (signup/verify-email/login/refresh/forgot-password/reset-password skip verifyCsrf; logout/change-password require it)
  - Atomic mutation + tokenVersion bump + cookie re-issue pattern (used by change-password; reset-password uses the bump but not the re-issue)
</output>
