---
phase: 01-auth-routes
verified: 2026-05-07T23:05:00Z
status: human_needed
score: 36/36 must-haves verified (gates green; CR-01 and WR-05 routed to human as deviations from must_have intent)
overrides_applied: 0
re_verification: # First verification — no prior VERIFICATION.md
  previous_status: null
gaps: [] # No artifact-level gaps; all files exist, are substantive, wired, and produce real data flow.
human_verification:
  - test: "CR-01 — forgot-password timing parity (D-23)"
    expected: "No-user branch and user-exists branch take indistinguishable wall-clock time over 1000+ runs (within bcrypt jitter, ±~50ms)"
    why_human: "REVIEW.md flags this as critical: no-user branch runs ONE bcrypt compare (~150-300ms), user branch runs prisma.$transaction with 2 inserts (~20-80ms). The two paths are NOT timing-symmetric, so a careful attacker can distinguish 'email exists' from 'email does not'. Plans/Summaries claim D-23 enumeration resistance but the implementation does not deliver it. Static analysis confirms the asymmetry; a quantitative timing-distribution test under realistic latency is needed to decide whether to (a) accept the residual leak as in-scope, (b) anchor both branches to a fixed deadline (REVIEW.md option A), or (c) run hashPassword on both branches (option B). Until then, AUTH-07 is functionally satisfied (200 + outbox event for known users, 200 + nothing for unknown) but the enumeration-resistance INVARIANT in the must_haves truth set is at risk."
  - test: "WR-05 — verify-email + reset-password code single-use under concurrency (TOCTOU)"
    expected: "Two simultaneous requests for the same valid VerificationCode result in exactly one cookie issuance / password reset; the second returns VERIFICATION_CODE_INVALID"
    why_human: "REVIEW.md WR-05 flags a TOCTOU window between findFirst({ usedAt: null }) and the subsequent verificationCode.update inside $transaction. The fix is to use updateMany({ where: { id, usedAt: null } }) and check affected count. Static analysis confirms the race window is real; a concurrent-request test against a real Postgres DB is needed to (a) reproduce the race, (b) decide whether the impact justifies a follow-up plan. Plan-level claim of 'single-use code consumption' (truth in 01-02 must_haves) is technically violated by the race, though the email channel itself is the practical attack surface — low real-world impact."
  - test: "End-to-end auth happy path against a running Next dev server"
    expected: "signup → outbox event drained → verify-email → cookies set → me returns user → logout clears cookies → login again → refresh rotates → change-password succeeds and current browser stays logged in"
    why_human: "Vitest mocks Prisma + cookies; no integration test runs the full HTTP stack. Phase 6 TEST-03 covers this formally; for Phase 1 closeout, a manual smoke test confirms the wiring works against a real Postgres + Redis stack."
  - test: "Login lockout actually fires under real Redis and clears via real TTL"
    expected: "5 wrong-password POSTs to /api/auth/login lock the account; subsequent attempt returns 423 LOCKED_OUT; lockout clears after AUTH_LOCKOUT_DURATION_MIN minutes"
    why_human: "Unit tests use vi.mock for getRedis/lockout; the real Redis sliding-window-counter + lockout-flag interaction was not exercised end-to-end. WR-04 also flags the in-memory fallback diverges from Redis behavior (separate flag vs counter-resetAt). A live Redis run validates AUTH-10 in production-shaped conditions."
  - test: "Refresh single-flight under real concurrency"
    expected: "Two simultaneous /api/auth/refresh calls with the same refresh cookie: exactly one returns 200 with new cookies, the other returns 409 CONFLICT"
    why_human: "Unit tests assert the lock acquire/release path; concurrent behavior against real Redis SETNX needs human-verified end-to-end run. Critical for AUTH-04 production correctness."
---

# Phase 1: Auth Routes Verification Report

**Phase Goal:** Users can authenticate — sign up, verify email, log in, stay logged in across sessions, log out, reset passwords, and change passwords — with full enumeration resistance and per-email rate limiting

**Verified:** 2026-05-07T23:05:00Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

Truths derived from ROADMAP Success Criteria + PLAN frontmatter must_haves (deduplicated). All artifact-level checks PASS; entries flagged with footnotes route to human verification.

| #   | Truth                                                                                                                                        | Status                       | Evidence                                                                                                                                                                                                                                                  |
| --- | -------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | `POST /api/auth/signup` returns identical 201 for new and existing emails (enumeration-resistant); no cookies set; VerificationCode created  | VERIFIED                     | `signup/route.ts:104-114` (existing → dummy bcrypt → 201) and `:121-142` (new → tx with user/code/outbox → 201). `setAuthCookies` not present. 8 tests green.                                                                                              |
| 2   | Signup runs `dummyBcryptCompare` on duplicate-email branch (D-22)                                                                            | VERIFIED                     | `signup/route.ts:109` calls `dummyBcryptCompare(password)` before returning 201. Test asserts the spy is invoked.                                                                                                                                         |
| 3   | Signup rejects banned/pwned/short passwords with stable error codes BEFORE user lookup                                                       | VERIFIED                     | `signup/route.ts:68-97` runs `isBanned` → length → `isPwned` (env-gated) BEFORE `prisma.user.findUnique` at line 104.                                                                                                                                       |
| 4   | `POST /api/auth/verify-email` with valid 8-char Crockford code marks usedAt + sets emailVerifiedAt + issues 3 cookies                        | VERIFIED                     | `verify-email/route.ts:128-146` runs $transaction (code.update usedAt + user.update emailVerifiedAt) then `setAuthCookies` + `setCsrfCookie`. 7 tests green.                                                                                              |
| 5   | verify-email rejects invalid codes (VERIFICATION_CODE_INVALID) and expired codes (VERIFICATION_CODE_EXPIRED)                                  | VERIFIED                     | `verify-email/route.ts:93-114`. Code-not-found and user-not-found both return INVALID (enumeration resistance).                                                                                                                                            |
| 6   | `POST /api/auth/login` issues 3 cookies on credential match                                                                                  | VERIFIED                     | `login/route.ts:144-151` calls createAccessToken/createRefreshToken/setAuthCookies/setCsrfCookie after successful verifyPassword.                                                                                                                          |
| 7   | Login returns INVALID_CREDENTIALS for unknown email AND wrong password (with dummy bcrypt on no-user branch)                                  | VERIFIED                     | `login/route.ts:108-114` (no-user → dummy → INVALID) and `:117-130` (wrong pw → recordFailure → INVALID).                                                                                                                                                  |
| 8   | Login returns EMAIL_NOT_VERIFIED only AFTER successful credential validation (D-24)                                                          | VERIFIED                     | `login/route.ts:132-139` — emailVerifiedAt check sits AFTER verifyPassword success at line 117.                                                                                                                                                            |
| 9   | Login records failed attempts via lockout-store; 5 fails → LOCKED_OUT (423)                                                                  | VERIFIED                     | `login/route.ts:84-90` (early lockout 423) and `:119-124` (recordFailure → LOCKED_OUT 423 if locked).                                                                                                                                                      |
| 10  | Login per-email rate limit returns 429 TOO_MANY_LOGIN_ATTEMPTS after 10/15m                                                                  | VERIFIED                     | `login/route.ts:43-52` createEmailLimiter with `AUTH_LOGIN_RATE_LIMIT_MAX ?? 10` + 15min window; `:77-81` enforces.                                                                                                                                        |
| 11  | `POST /api/auth/refresh` rotates access token via Redis SETNX single-flight lock                                                             | VERIFIED                     | `refresh/route.ts:71-94` acquireRefreshLock + try/finally release; CONFLICT 409 on contention.                                                                                                                                                            |
| 12  | Refresh validates tokenVersion against DB; mismatch → 401                                                                                    | VERIFIED                     | `refresh/route.ts:62-68` `if (user.tokenVersion !== payload.tokenVersion)` returns INVALID_REFRESH 401.                                                                                                                                                    |
| 13  | Refresh path-scoped to /api/auth (refresh cookie remains scoped)                                                                             | VERIFIED                     | `refresh/route.ts:36` reads `req.cookies.get(REFRESH_COOKIE_NAME)`; setAuthCookies in `lib/server/auth.ts` (do-not-modify) handles path scoping. Phase 0 + auth.ts contract covers this.                                                                  |
| 14  | `POST /api/auth/logout` requires CSRF + clears all 3 cookies                                                                                 | VERIFIED                     | `logout/route.ts:18-25` verifyCsrf gate + clearAuthCookies + clearCsrfCookie.                                                                                                                                                                              |
| 15  | `GET /api/auth/me` returns `{ user: { sub, email } }` via requireAuth; 401 without cookie                                                    | VERIFIED                     | `me/route.ts:21-29` requireAuth + 401 short-circuit + 200 with user identity.                                                                                                                                                                              |
| 16  | `POST /api/auth/forgot-password` returns 200 regardless of email existence; outbox event only when user exists                               | VERIFIED (intent — see #17)  | `forgot-password/route.ts:62-71` (no-user → dummy → 200) and `:73-95` (user → tx with code+outbox → 200). Both paths return identical body.                                                                                                                |
| 17  | forgot-password no-user branch runs `dummyBcryptCompare` for timing parity (D-23)                                                            | PARTIAL (CR-01)              | `forgot-password/route.ts:66` calls `dummyBcryptCompare(email)`. **REVIEW.md CR-01 flags asymmetry:** dummy bcrypt (~150-300ms) does NOT match the time of the user-exists tx ($transaction with 2 row inserts, ~20-80ms). Routed to human (see below). |
| 18  | `POST /api/auth/reset-password` consumes code + updates passwordHash + bumps tokenVersion in single tx                                       | VERIFIED                     | `reset-password/route.ts:147-159` $transaction with user.update (passwordHash + tokenVersion: { increment: 1 }) + verificationCode.update (usedAt). No setAuthCookies (user must log in fresh).                                                            |
| 19  | reset-password rejects banned/short passwords with stable error codes                                                                        | VERIFIED                     | `reset-password/route.ts:64-93`.                                                                                                                                                                                                                          |
| 20  | `PUT /api/auth/change-password` requires CSRF + requireAuth                                                                                  | VERIFIED                     | `change-password/route.ts:60-72` (CSRF first, then requireAuth).                                                                                                                                                                                          |
| 21  | change-password validates currentPassword via verifyPassword                                                                                 | VERIFIED                     | `change-password/route.ts:128-131`.                                                                                                                                                                                                                       |
| 22  | change-password enforces password policy on newPassword (length, banned, HIBP env-gated)                                                     | VERIFIED                     | `change-password/route.ts:85-109`.                                                                                                                                                                                                                        |
| 23  | change-password hashes newPassword + bumps user.tokenVersion atomically                                                                      | VERIFIED                     | `change-password/route.ts:136-144` single user.update with passwordHash + `tokenVersion: { increment: 1 }`.                                                                                                                                               |
| 24  | change-password issues NEW access + refresh + csrf cookies after success (Pitfall 9 — current browser stays logged in)                       | VERIFIED                     | `change-password/route.ts:150-157` createAccessToken/createRefreshToken/setAuthCookies/setCsrfCookie with the bumped tokenVersion.                                                                                                                          |
| 25  | Other sessions (different browser) fail next requireAuth call due to tokenVersion mismatch                                                   | VERIFIED                     | `requireAuth` (do-not-modify middleware) re-queries User.tokenVersion on every call; bumped tokenVersion at change-password line 137-144 invalidates other sessions on next request.                                                                       |
| 26  | All 9 routes export `runtime='nodejs'`                                                                                                       | VERIFIED                     | `grep -c "runtime = 'nodejs'"` on all 9 routes returns 9.                                                                                                                                                                                                  |
| 27  | All 9 routes wrap handler body in `withRequestContext(makeRequestContext(req.headers), ...)`                                                 | VERIFIED                     | All 9 routes import + invoke withRequestContext.                                                                                                                                                                                                          |
| 28  | All mutating routes that need CSRF call verifyCsrf before business logic                                                                     | VERIFIED                     | logout (`:18`), change-password (`:60`). Other routes are documented pre-session carve-outs with inline justification comments.                                                                                                                            |
| 29  | All 4 account-creation routes apply per-email rate limit via createEmailLimiter (D-08 values)                                                 | VERIFIED                     | signup (5/1h), verify (5/15m), forgot (3/1h), reset (5/15m) — each has module-level createEmailLimiter + per-request limiter.check.                                                                                                                      |
| 30  | 6 lib helpers exist under lib/server/auth/ (banned-passwords, hibp, lockout, refresh-lock, dummy-bcrypt, email-templates)                    | VERIFIED                     | All 6 files present + tests pass. lockout uses Redis with memory fallback; refresh-lock uses SETNX + Lua; dummy-bcrypt cost-12.                                                                                                                            |
| 31  | OutboxEvent union extended with email.verification_code + email.password_reset variants                                                       | VERIFIED                     | `outbox/types.ts` has both variants; dispatcher.ts handles both.                                                                                                                                                                                          |
| 32  | vitest.setup.ts sets JWT_SECRET / ENCRYPTION_KEY before any module loads                                                                     | VERIFIED                     | File exists; fixture passes auth.ts placeholder regex.                                                                                                                                                                                                    |
| 33  | `pnpm --filter frontend test` exits 0 with all suite tests green                                                                             | VERIFIED                     | 138/138 passed across 22 test files. Live run captured 2026-05-07T23:03Z.                                                                                                                                                                                  |
| 34  | `pnpm --filter frontend typecheck` exits 0                                                                                                   | VERIFIED                     | Live run exit 0.                                                                                                                                                                                                                                          |
| 35  | `pnpm --filter frontend lint` exits 0                                                                                                        | VERIFIED                     | Live run exit 0.                                                                                                                                                                                                                                          |
| 36  | `.env.example` documents 11 new AUTH_* vars + PASSWORD_HIBP_CHECK with sensible defaults + decision-ID rationale                              | VERIFIED                     | All 12 env tokens present at repo-root `.env.example`.                                                                                                                                                                                                    |

**Score:** 36/36 truths VERIFIED at the static-artifact level. **Truth #17 (forgot-password timing parity) is technically PARTIAL** — the dummy bcrypt is invoked but the timing is asymmetric per CR-01. Routed to human verification rather than failed because (a) the route still meets the AUTH-07 success-criteria response shape, (b) the residual leak is timing-only and requires the developer to decide between accept/fix-A/fix-B per REVIEW.md.

### Required Artifacts

All 9 route handlers + 6 lib helpers + 6 test infra files verified at all 4 levels (exists, substantive, wired, data-flow).

| Artifact                                                          | Expected                                          | Status      | Details                                                                                                                                                                                                  |
| ----------------------------------------------------------------- | ------------------------------------------------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `frontend/src/app/api/auth/signup/route.ts`                       | POST handler — AUTH-01                            | VERIFIED    | 149 lines, exports POST, runtime nodejs, dummy bcrypt + outbox + tx all wired.                                                                                                                          |
| `frontend/src/app/api/auth/verify-email/route.ts`                 | POST handler — AUTH-03                            | VERIFIED    | 156 lines, exports POST, $transaction wires usedAt + emailVerifiedAt + 3 cookies.                                                                                                                       |
| `frontend/src/app/api/auth/login/route.ts`                        | POST handler — AUTH-02 + AUTH-10 lockout          | VERIFIED    | 158 lines, lockout integrated (isLockedOut/recordFailure/recordSuccess), dummy bcrypt on no-user branch.                                                                                                |
| `frontend/src/app/api/auth/refresh/route.ts`                      | POST handler — AUTH-04 single-flight              | VERIFIED    | 96 lines, acquireRefreshLock + finally release, tokenVersion DB check.                                                                                                                                  |
| `frontend/src/app/api/auth/logout/route.ts`                       | POST handler — AUTH-05                            | VERIFIED    | 32 lines, verifyCsrf + clearAuthCookies + clearCsrfCookie.                                                                                                                                              |
| `frontend/src/app/api/auth/me/route.ts`                           | GET handler — AUTH-06                             | VERIFIED    | 31 lines, requireAuth + 401 short-circuit.                                                                                                                                                              |
| `frontend/src/app/api/auth/forgot-password/route.ts`              | POST handler — AUTH-07                            | VERIFIED    | 99 lines, dummy bcrypt + outbox event. Timing-asymmetric per CR-01.                                                                                                                                     |
| `frontend/src/app/api/auth/reset-password/route.ts`               | POST handler — AUTH-08                            | VERIFIED    | 166 lines, tokenVersion bump + code consumption in same tx.                                                                                                                                             |
| `frontend/src/app/api/auth/change-password/route.ts`              | PUT handler — AUTH-09                             | VERIFIED    | 165 lines, CSRF + requireAuth + atomic update + new cookies.                                                                                                                                            |
| `frontend/src/lib/server/auth/banned-passwords.ts`                | isBanned                                          | VERIFIED    | ~95-entry banned list, lower-cased comparison.                                                                                                                                                          |
| `frontend/src/lib/server/auth/hibp.ts`                            | isPwned + pwnedCount                              | VERIFIED    | k-anonymity + Add-Padding header + 2s AbortController + fail-open.                                                                                                                                      |
| `frontend/src/lib/server/auth/lockout.ts`                         | isLockedOut/recordFailure/recordSuccess            | VERIFIED    | Redis sliding-window + memory fallback. WR-04 flags fallback asymmetry — info-level deviation, not a phase blocker.                                                                                      |
| `frontend/src/lib/server/auth/refresh-lock.ts`                    | acquireRefreshLock                                | VERIFIED    | SETNX + Lua compare-and-delete, 5s TTL.                                                                                                                                                                 |
| `frontend/src/lib/server/auth/dummy-bcrypt.ts`                    | dummyBcryptCompare                                | VERIFIED    | Cost-12 dummy hash literal `$2a$12$...`.                                                                                                                                                                |
| `frontend/src/lib/server/auth/email-templates.ts`                 | verificationEmail + resetPasswordEmail            | VERIFIED    | Both factories return non-empty {subject, html, text}. WR-03 flags missing HTML escape — info-level (today the only caller passes regex-validated codes).                                                |
| `frontend/vitest.setup.ts`                                        | JWT_SECRET / ENCRYPTION_KEY fixtures              | VERIFIED    | Sets all 4 env vars; fixture string bypasses auth.ts placeholder regex.                                                                                                                                 |
| `frontend/src/test-utils/prisma-mock.ts`                          | Shared mockDeep + auto-reset                      | VERIFIED    | Vi.mock at module level + beforeEach mockReset.                                                                                                                                                         |
| `frontend/src/lib/server/outbox/types.ts`                         | Email variants                                    | VERIFIED    | EmailVerificationCodeEvent + EmailPasswordResetEvent in union.                                                                                                                                          |
| `.env.example`                                                    | 11 AUTH_* + PASSWORD_HIBP_CHECK                   | VERIFIED    | All entries present at repo root.                                                                                                                                                                       |

### Key Link Verification

| From                              | To                                       | Via                                                              | Status   | Details                                                                                                                                  |
| --------------------------------- | ---------------------------------------- | ---------------------------------------------------------------- | -------- | ---------------------------------------------------------------------------------------------------------------------------------------- |
| signup/route.ts                   | dummy-bcrypt.ts                          | dummyBcryptCompare on existing-email branch (D-22)               | WIRED    | `signup/route.ts:109`                                                                                                                    |
| signup/route.ts                   | outbox/index.ts                          | enqueueOutbox(tx, { kind: 'email.verification_code', ... })      | WIRED    | `signup/route.ts:134-141`                                                                                                                |
| verify-email/route.ts             | auth.ts                                  | setAuthCookies + setCsrfCookie + timingSafeCompare               | WIRED    | `verify-email/route.ts:139-146` and `:116`                                                                                              |
| reset-password/route.ts           | User.tokenVersion                        | tokenVersion: { increment: 1 } in same tx as passwordHash        | WIRED    | `reset-password/route.ts:152`                                                                                                            |
| forgot-password/route.ts          | dummy-bcrypt.ts                          | dummyBcryptCompare on no-user branch (D-23)                      | WIRED*   | `forgot-password/route.ts:66`. *Timing parity not achieved per CR-01 — see human verification.                                          |
| login/route.ts                    | lockout.ts                               | isLockedOut + recordFailure + recordSuccess                      | WIRED    | `login/route.ts:84, 119, 142`                                                                                                            |
| login/route.ts                    | dummy-bcrypt.ts                          | dummyBcryptCompare on no-user branch (D-24)                      | WIRED    | `login/route.ts:109`                                                                                                                    |
| refresh/route.ts                  | refresh-lock.ts                          | acquireRefreshLock — release in finally                          | WIRED    | `refresh/route.ts:71` + `:92-94`                                                                                                         |
| refresh/route.ts                  | User.tokenVersion (DB)                   | Compare JWT.tokenVersion against users.tokenVersion              | WIRED    | `refresh/route.ts:62-68`                                                                                                                 |
| logout/route.ts                   | auth.ts                                  | verifyCsrf + clearAuthCookies + clearCsrfCookie                  | WIRED    | `logout/route.ts:18, 24-25`                                                                                                              |
| me/route.ts                       | middleware/index.ts                      | requireAuth — DB-side tokenVersion check                         | WIRED    | `me/route.ts:21`                                                                                                                         |
| change-password/route.ts          | auth.ts                                  | verifyCsrf + verifyPassword + hashPassword + setAuth/CsrfCookies | WIRED    | `change-password/route.ts:60, 128, 136, 156-157`                                                                                         |
| change-password/route.ts          | User.tokenVersion (DB)                   | Same single user.update — `tokenVersion: { increment: 1 }`        | WIRED    | `change-password/route.ts:141`                                                                                                           |
| change-password/route.ts          | banned-passwords.ts + hibp.ts             | Password policy gates BEFORE DB lookup                           | WIRED    | `change-password/route.ts:94, 102` (before line 114 user.findUnique)                                                                      |
| vitest.config.ts                  | vitest.setup.ts                          | test.setupFiles array                                            | WIRED    | setupFiles: ['./vitest.setup.ts']                                                                                                        |
| lockout.ts                        | redis.ts                                 | getRedis() + memory fallback                                     | WIRED    | Redis path uses incr + expire + lockout flag; memory path uses Map.                                                                       |
| refresh-lock.ts                   | redis.ts                                 | redis.set + redis.eval (Lua compare-and-delete)                  | WIRED    | SETNX with NX+EX; release Lua script.                                                                                                    |

### Data-Flow Trace (Level 4)

Routes return JSON only (no UI rendering). Data flows from DB through Prisma → route handler → JSON response. Verified via test cases (138 passing) that mock Prisma and assert on response shape + DB call arguments. Outbox events flow through `enqueueOutbox` into `OutboxEvent` rows (Phase 5 cron drains them). Cookie data flows from `auth.ts` setters into `next/headers cookies()` store.

| Artifact                          | Data Variable                | Source                                         | Produces Real Data                | Status   |
| --------------------------------- | ---------------------------- | ---------------------------------------------- | --------------------------------- | -------- |
| signup → outbox event             | code, expiresAt              | generateVerificationCode + Date.now()           | Real                              | FLOWING  |
| verify-email → cookies            | accessToken, refreshToken     | createAccessToken/createRefreshToken            | Real (signed JWTs from auth.ts)   | FLOWING  |
| login → cookies + user payload    | accessToken, refreshToken, user | DB user + JWT signing                          | Real                              | FLOWING  |
| refresh → new tokens              | accessToken, refreshToken     | DB user.tokenVersion + JWT signing              | Real                              | FLOWING  |
| me → user payload                 | { sub, email }                | requireAuth + DB tokenVersion check             | Real                              | FLOWING  |
| forgot-password → outbox event    | code, expiresAt              | generateVerificationCode + DB user existence    | Real (when user exists)           | FLOWING  |
| reset-password → DB write         | passwordHash, tokenVersion+1  | hashPassword + Prisma                           | Real                              | FLOWING  |
| change-password → DB + new cookies | passwordHash, tokenVersion+1, new JWTs | hashPassword + Prisma + JWT signing           | Real                              | FLOWING  |

No HOLLOW or DISCONNECTED paths found.

### Behavioral Spot-Checks

Phase produces server-side route handlers; no UI to render. Spot-checks limited to test-suite execution + grep-based artifact assertions.

| Behavior                                                              | Command                                                              | Result                                | Status   |
| --------------------------------------------------------------------- | -------------------------------------------------------------------- | ------------------------------------- | -------- |
| Vitest suite runs all auth tests green                                | `pnpm --filter frontend test`                                         | 138/138 passed across 22 test files    | PASS     |
| Typecheck clean                                                       | `pnpm --filter frontend typecheck`                                    | exit 0                                | PASS     |
| Lint clean                                                            | `pnpm --filter frontend lint`                                         | exit 0                                | PASS     |
| All 9 auth routes export `runtime='nodejs'`                           | `grep -c "runtime = 'nodejs'" frontend/src/app/api/auth/*/route.ts`   | 9                                     | PASS     |
| All 9 auth routes wrap handler in withRequestContext                  | `grep -l withRequestContext .../*/route.ts \| wc -l`                  | 9                                     | PASS     |
| .env.example contains all 11 AUTH_* + PASSWORD_HIBP_CHECK              | grep `(AUTH_\|PASSWORD_HIBP)` .env.example                            | 12 entries (11 AUTH_* + 1 HIBP)       | PASS     |
| Live HTTP smoke (signup → verify → me → logout)                       | n/a — would require dev server + DB + Redis                          | not run                               | SKIP (routed to human)  |
| Live concurrent /refresh single-flight                                 | n/a — would require dev server + Redis                              | not run                               | SKIP (routed to human)  |
| Live timing comparison forgot-password no-user vs user-exists          | n/a — needs Postgres + repeated runs                                  | not run                               | SKIP (routed to human, CR-01)  |

### Requirements Coverage

| Requirement | Source Plan       | Description                                                                       | Status     | Evidence                                                                                                                              |
| ----------- | ----------------- | --------------------------------------------------------------------------------- | ---------- | ------------------------------------------------------------------------------------------------------------------------------------- |
| AUTH-01     | 01-02-PLAN        | Sign up enumeration-resistant, no cookies, dummy bcrypt non-existent emails       | SATISFIED  | signup/route.ts; truths #1-3 verified; 8 tests green                                                                                  |
| AUTH-02     | 01-03-PLAN        | Login + 3 cookies (access 15m + refresh 7d /api/auth + CSRF 7d)                   | SATISFIED  | login/route.ts; truths #6-10 verified                                                                                                 |
| AUTH-03     | 01-02-PLAN        | Verify email via 8-char Crockford code → cookies issued                            | SATISFIED  | verify-email/route.ts; truths #4-5 verified                                                                                           |
| AUTH-04     | 01-03-PLAN        | Refresh single-flight; refresh cookie path-scoped /api/auth                       | SATISFIED  | refresh/route.ts; truths #11-13 verified                                                                                              |
| AUTH-05     | 01-03-PLAN        | Logout clears all auth cookies                                                    | SATISFIED  | logout/route.ts; truth #14 verified                                                                                                   |
| AUTH-06     | 01-03-PLAN        | GET /api/auth/me with requireAuth                                                 | SATISFIED  | me/route.ts; truth #15 verified                                                                                                       |
| AUTH-07     | 01-02-PLAN        | Forgot-password — always 200 (no enumeration leak)                                | SATISFIED* | forgot-password/route.ts; truth #16 verified for response-shape; *truth #17 timing parity routed to human (CR-01)                      |
| AUTH-08     | 01-02-PLAN        | Reset-password using emailed code + new password                                  | SATISFIED  | reset-password/route.ts; truths #18-19 verified                                                                                       |
| AUTH-09     | 01-04-PLAN        | Authenticated change-password bumps tokenVersion + requires CSRF                  | SATISFIED  | change-password/route.ts; truths #20-25 verified                                                                                      |
| AUTH-10     | 01-01,03,05-PLANs | Per-email rate limiting + failed-login lockout                                    | SATISFIED  | createEmailLimiter on all relevant routes (D-08 values); lockout on login (D-07); env-tunable defaults documented in .env.example   |

All 10 requirements account for; no orphans (REQUIREMENTS.md maps AUTH-01..10 to Phase 1, all appear in at least one plan).

### Anti-Patterns Found

Cross-referenced REVIEW.md (1 critical, 7 warnings, 6 info) against the must_haves. Categorized for phase-blocker vs follow-up.

| File                                              | Line   | Pattern                                                                  | Severity | Impact on phase goal                                                                                              |
| ------------------------------------------------- | ------ | ------------------------------------------------------------------------ | -------- | ----------------------------------------------------------------------------------------------------------------- |
| forgot-password/route.ts                          | 62-92  | CR-01 timing-asymmetric branches (dummy bcrypt vs $transaction)          | Warning  | Violates D-23 enumeration-resistance INVARIANT but not the AUTH-07 response-shape success criterion. **Routed to human.** |
| reset-password/route.ts                           | 62-96  | WR-01 password policy runs BEFORE rate limiter — HIBP probing via email rotation | Warning  | Doesn't violate any must_have; HIBP-probe attack only meaningful when PASSWORD_HIBP_CHECK=1 (off by default). Follow-up. |
| change-password/route.ts, reset-password/route.ts | n/a    | WR-02 lockout counter not reset on successful password change            | Warning  | UX foot-gun (lock out a user with new password if old password near threshold). Not a goal-blocker. Follow-up.     |
| email-templates.ts                                | 29, 37 | WR-03 raw `${args.code}` in HTML without escape (XSS-safe today)         | Info     | Future-proofing only; current callers pass regex-validated codes. Follow-up.                                       |
| lockout.ts                                        | 80-89  | WR-04 in-memory fallback uses counter resetAt as lockout window          | Info     | Dev-only divergence from Redis behavior. Follow-up.                                                               |
| signup/route.ts, verify-email/route.ts            | 104-142 / 66-137 | WR-05 TOCTOU — code update not gated on `usedAt: null`            | Warning  | Race-condition: same code redeemable twice under concurrency. Email channel itself is the realistic attack surface. **Routed to human.** |
| refresh/route.ts                                  | 79-94  | WR-06 old refresh token not invalidated post-rotation                    | Info     | Documented stateless-refresh tradeoff; D-19 already accepts this. Follow-up doc-only.                              |
| (multiple)                                        | n/a    | WR-07 rate-limiter falls back to memory in dev/serverless without Redis  | Info     | Phase 0 + CLAUDE.md already describe this; production runs need Redis. Follow-up.                                  |
| change-password/route.ts                          | 85     | IN-01 reads env per-request vs module-level in signup/reset               | Info     | Inconsistency, not a defect. Follow-up.                                                                            |
| dummy-bcrypt.ts                                   | 18     | IN-02 hash literal hand-rolled, no compile-time link to auth.ts cost     | Info     | Phase 1 mitigation (`$2a$12$` source-grep test) catches drift. Follow-up.                                          |
| verify-email/route.ts                             | 115-126 | IN-03 redundant timingSafeCompare after exact-match query               | Info     | Defense-in-depth, intentional. Document.                                                                           |
| outbox/dispatcher.ts                              | 151, 160 | IN-04 dynamic import of email-templates                                  | Info     | Style. Follow-up.                                                                                                  |
| package.json                                      | 10     | IN-05 test:integration is a noop with exit 0                              | Info     | Intentional per D-26 (deferred to Phase 4). Document with grep-able TODO.                                         |
| vitest.setup.ts                                   | 13-16  | IN-06 `||=` keeps stale env from prior process                            | Info     | Test-environment foot-gun. Follow-up.                                                                              |

**Phase-blocker classification:** None. All findings are either follow-up work (truly non-blocking warnings/info) or routed to human verification (CR-01, WR-05).

### Human Verification Required

5 items need human testing, listed in YAML frontmatter. Summary:

1. **CR-01 — forgot-password timing parity** (D-23): static evidence shows asymmetric code paths between no-user (single bcrypt) and user-exists ($transaction with 2 inserts). A timing-distribution measurement under realistic latency is needed to decide accept/fix.

2. **WR-05 — verify-email + reset-password code single-use TOCTOU**: race between findFirst({ usedAt: null }) and the subsequent update. Concurrent-request test against Postgres needed to confirm impact and decide on the updateMany-with-affected-count fix.

3. **End-to-end auth happy path against dev server**: Vitest mocks Prisma + cookies; no integration test runs the full HTTP stack. Phase 6 TEST-03 covers this formally; Phase 1 closeout benefits from a manual smoke against real Postgres + Redis.

4. **Lockout under real Redis + TTL**: unit tests use vi.mock; live Redis run validates AUTH-10 in production-shaped conditions and verifies WR-04's in-memory-fallback divergence is not a blocker.

5. **Refresh single-flight under real concurrency**: unit tests assert lock acquire/release; concurrent-request behavior against real Redis SETNX needs human-verified end-to-end run.

### Gaps Summary

**No artifact-level gaps.** All 36 must_haves have implementation evidence; all 9 routes + 6 lib helpers + test infra files exist, are substantive, are wired, and produce real data flow. The full test gate (138 tests, typecheck, lint, format) is green at the time of verification.

**Two REVIEW findings tension against must_haves but do not block goal achievement at the static-artifact level:**

1. **CR-01 (critical):** The forgot-password route invokes `dummyBcryptCompare` on the no-user branch and runs a `$transaction` on the user-exists branch — both branches return identical 200 responses (so AUTH-07 success criterion is met), but the timing is not symmetric (so the D-23 ENUMERATION RESISTANCE INVARIANT — must_have truth #17 — is at risk). REVIEW.md proposes two fixes (anchor-deadline or hashPassword-on-both). Decision is the developer's: accept the residual leak, or schedule a follow-up plan.

2. **WR-05 (warning):** verify-email and reset-password follow `findFirst({ usedAt: null })` → `update` outside a guarded WHERE. Under concurrency, the same code can be redeemed twice. The plan-stated "single-use" property (truth #4 / 01-02 must_have) holds in single-request flow but fails under concurrent-request flow. Email transit is the realistic exfil vector; impact is low but not zero. REVIEW.md proposes the updateMany-with-affected-count fix as a follow-up.

Phase status is **human_needed** rather than **gaps_found** because:
- All routes function correctly per their tests and acceptance criteria
- The identified deviations are quality/security-hardening items the developer should consciously decide upon
- REVIEW.md surfaces these explicitly; the developer can accept (CR-01 → option A), schedule a follow-up plan (CR-01 → option B), or close out the phase as-is and address in Phase 7 quality gate.

---

_Verified: 2026-05-07T23:05:00Z_
_Verifier: Claude (gsd-verifier)_
