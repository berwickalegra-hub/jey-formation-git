// Wave 0 RED scaffold for ADMIN-02 (orders list + filters).
// See sibling users/route.test.ts header for the it.todo rationale.
import { describe, it } from 'vitest';

describe('/api/admin/orders [Wave 1] — list', () => {
  it.todo('GET returns paginated orders for ADMIN');
  it.todo('GET filters by status and since/until');
});
